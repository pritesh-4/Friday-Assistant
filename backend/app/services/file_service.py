
class FileService:
    """
    Service responsible for handling file uploads, document indexing, and parsing.
    Currently implemented as a stub placeholder.
    """
    
    async def save_uploaded_file(self, filename: str, file_data: bytes) -> str:
        """
        Save an uploaded file and return its storage URI or identifier.
        """
        return ""
